<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.agripricechecker">

    <!-- Required permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:label="Agri Price Checker"
        android:icon="@mipmap/app"
        android:roundIcon="@mipmap/app"
        android:theme="@style/Theme.AgriPriceChecker"
        android:usesCleartextTraffic="true">

        <!-- ✅ Splash Screen is the new launcher -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AgriPriceChecker">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Other activities -->
        <activity android:name=".LoginActivity" android:exported="false" />
        <activity android:name=".SignupActivity" android:exported="false" />
        <activity android:name=".MainActivity" android:exported="false" />
        <activity android:name=".FetchPriceActivity" />
        <activity android:name=".ChartCropSelectionActivity" />
        <activity android:name=".ChartActivity" />
        <activity android:name=".CropCalendarActivity" />
        <activity android:name=".CropDetailActivity" />
        <activity android:name=".FertilizerActivity" />
        <activity android:name=".FertilizerDetailActivity" />

    </application>

</manifest>
